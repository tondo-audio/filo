#pragma once

#include <juce_gui_basics/juce_gui_basics.h>

namespace filo::icons
{
    juce::Path dragHandle();
    juce::Path power(bool filled);
    juce::Path cross();
    juce::Path plus();
    juce::Path refresh();
    juce::Path monitor();
    juce::Path chevronDown();
    juce::Path clip();
}
